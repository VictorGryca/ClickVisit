<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Imóveis Associados - <%= broker.name %></title>
  <style>
    table { border-collapse: collapse; }
    th, td { padding: .4rem .8rem; border: 1px solid #666; }
  </style>
</head>
<body>
  <div style="text-align:right;">
    <a href="/logout"><button>Logout</button></a>
  </div>
  <h1>Imóveis Associados ao Corretor <%= broker.name %></h1>
  <a href="/brokers/<%= broker.id %>"><button>Voltar ao Perfil</button></a>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Endereço</th>
        <th>Preço</th>
        <th>Status</th>
        <th>Agência</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% if (properties.length === 0) { %>
        <tr><td colspan="6">Nenhum imóvel associado.</td></tr>
      <% } %>
      <% properties.forEach(p => { %>
        <tr>
          <td><%= p.id %></td>
          <td><%= p.address %></td>
          <td>R$ <%= p.price.toLocaleString('pt-BR') %></td>
          <td><%= p.status %></td>
          <td><%= p.agency_name %></td>
          <td>
            <a href="/client/calendar/<%= broker.id %>/<%= p.id %>">
              <button type="button">Ver Agenda Cliente</button>
            </a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
